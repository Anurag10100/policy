# Claude API key for policy summarization
ANTHROPIC_API_KEY=sk-ant-...

# Database (PostgreSQL) - for Phase 2
# DATABASE_URL=postgresql://user:pass@localhost:5432/policyai

# Email service (for newsletter delivery)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
